<Application
    RequestedThemeVariant="Light"
    x:Class="PatasePasseios.View.App"
    xmlns="https://github.com/avaloniaui"
    xmlns:framework="using:AvaloniaFramework"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        The starting variant only. ClassicalTheme now carries a full Dark dictionary, so following
        the OS no longer strands Fluent's dark combobox and calendar popups against hardcoded light
        surfaces — the reason this was pinned. Ajustes will write Application.Current.RequestedThemeVariant
        and this becomes the value used until the stored preference is read.
    -->

    <!--
        Fluent derives selection rings, focus adorners and the calendar/clock highlights from its
        own accent. Repointing it at the Classical brown keeps the parts of those popups that are
        not restyled in ClassicalTheme.axaml on-palette rather than Windows blue.
        Light/dark variants exist because Fluent looks them up per theme variant.
    -->
    <Application.Resources>
        <ResourceDictionary>
            <!--
                Type. Seven roles from the refresh's ramp, in app units: the design's px times 1.7476
                (720 canvas over the design's 412) times a further 1.1 for text, per this repo's
                existing convention — so design 18 body lands on 35, which is where the app's own body
                already was. Sizes are Doubles rather than literals in the views so the six-step size
                control has one place to write to.
            -->
            <x:Double x:Key="TypeDisplay">81</x:Double>
            <x:Double x:Key="TypeTitle">58</x:Double>
            <x:Double x:Key="TypeSection">42</x:Double>
            <x:Double x:Key="TypeBody">35</x:Double>
            <x:Double x:Key="TypeUi">29</x:Double>
            <x:Double x:Key="TypeCaption">25</x:Double>
            <x:Double x:Key="TypeMicro">21</x:Double>

            <Color x:Key="SystemAccentColor">#B68235</Color>
            <Color x:Key="SystemAccentColorLight1">#C28D41</Color>
            <Color x:Key="SystemAccentColorLight2">#E1AD66</Color>
            <Color x:Key="SystemAccentColorLight3">#FACB8D</Color>
            <Color x:Key="SystemAccentColorDark1">#A06F24</Color>
            <Color x:Key="SystemAccentColorDark2">#7D5411</Color>
            <Color x:Key="SystemAccentColorDark3">#5A3B0A</Color>
        </ResourceDictionary>
    </Application.Resources>

    <Application.Styles>
        <FluentTheme />
        <framework:LayoutStyles />
        <StyleInclude Source="Components/ClassicalTheme.axaml" />
    </Application.Styles>

</Application>